{
  "_v": 3,
  "alias": "http-stop-headers",
  "alias_explicit": false,
  "column": "p1",
  "created_at_millis": 1786343579255,
  "creator": "Alexander Terp",
  "description": "Passing `headers=` alongside `json=` drops the automatic `Content-Type: application/json`, so Jira answers 415. One condition at `core/funcs.go:1951-1953`: `if headersArg.IsNull()` wants to be \"unless Content-Type is already set\".\n\nDocumented as a wart twice in live scripts - `raiselog:120-121` (\"it must be given explicitly - Jira 415s otherwise\") and `snapstore-push:29` - and `work:274` pays the same tax. The review called it the clearest single fix in its report.\n\n---\n\n### Orientation\n- Confirmed: the condition is exactly where the card says. `core/funcs.go:1951-1953` is `if headersArg.IsNull() { headers[\"Content-Type\"] = ... }`, inside the shared `Execute` closure that all nine `http_*` builtins are built from by `createHttpFunctions()` (`core/funcs.go:1890-1966`).\n- One edit fixes all nine methods.\n- The user's `headers=` map is flattened into the Go `headers` map just above, at `core/funcs.go:1917-1932`.\n\n### Watch out for\n- There is no existing helper that asserts on request headers - `core/testing/test_helpers.go` only has `assertHttpInvocationUrls` (:568) and `assertHttpInsecure` (:590). Either add a header assert helper next to them or read `httpInvocations[i].RequestDef.Headers` directly.\n- `core/requester.go:178-182` applies headers with `req.Header.Add`, which canonicalizes the key. If the map ends up holding both `content-type` and `Content-Type`, both get added under the same canonical name and the request goes out with a duplicated Content-Type. The case-insensitive check above is what prevents that.\n- Stay behind the `RReq` seam (`core/global.go:44`) - `core/testing/` injects a Requester with mocked responses.\n",
  "effort": "xs",
  "id": "a_25NhyP93i",
  "labels": [
    "ai-hi"
  ],
  "position": "UU",
  "source": [
    "takehome"
  ],
  "title": "http: stop headers= suppressing the json= Content-Type",
  "type": "bug",
  "updated_at_millis": 1786355430705,
  "history": [
    {"field":"column","value":"p1","at":1786343579255}
  ]
}