{
  "_v": 3,
  "alias": "let-an-unambiguous",
  "alias_explicit": false,
  "column": "done",
  "comments": [
    {
      "author": "Alexander Terp",
      "body": "Code-complete, blocked on a tree-sitter-rad release.\n\nLanded the card's original two-case rule, not the wider one: a `{` is literal when the string ends immediately after it, or when the `{...}` pair holds only whitespace. Everything else stays an interpolation.\n\nTwo findings that moved the design:\n\n1. Allowing whitespace before the end-of-string case (a \"cleaner\" single rule) also swallows `\"{ \"a\" + \"b\" }\"`, which parses today. The card's two-case split preserves it at no cost.\n\n2. The end-of-string case necessarily makes `\"{\"ERROR\".red()}\"` illegal - `\"{\"` is the identical prefix for both readings and nothing before the closing quote separates them. This idiom was live in rad's own `dev` script (2 sites, now fixed to `{'ERROR'.red()}`) and in `magpie/src/movies.rad:124` (outside this repo, still needs `\"{\"$1\"}=REDACTED\"` -\u003e `\"{'$1'}=REDACTED\"`). Alex's call was that the idiom should be illegal anyway - use a different quote type. Measured with a before/after binary over the invocation-log corpus: those were the only two source files affected.\n\n`\\}` from the bugfixes board rode along, since it is the same grammar release. `\\{` and `\\}` are now symmetric.\n\nImplementation is entirely `src/scanner.c` for the literal brace - no new node kind, so no `rl/consts.go` or converter change for that half. `\\}` did need both. Work is in a worktree at `~/src/tree-sitter-rad-braces` off clean `v0.6.0`; the main checkout was left untouched because another session is live in it.\n\nRemaining: tag and release the grammar, then drop the `replace` directive and bump `go.mod`.",
      "created_at_millis": 1786359458117,
      "id": "c_25UN3TAQF"
    },
    {
      "author": "Alexander Terp",
      "body": "Grammar committed: `1e41475` on tree-sitter-rad main, on top of the `?? -1` work another session landed there. Rad side is `cc9dec5d` on rad-sprint5, with no replace directive.\n\nState: the brace tests in rad are red until tree-sitter-rad is tagged and `go.mod` moves off v0.6.0. Verified green when go.mod points at the grammar commit. The RAD40017 half (`rts/check`) is grammar-independent and passes as committed.",
      "created_at_millis": 1786359956938,
      "id": "c_25Ua21DmM"
    }
  ],
  "created_at_millis": 1786353931892,
  "creator": "Alexander Terp",
  "description": "`print(\"{\")` is a syntax error. It shouldn't be - there is no interpolation there to fail at.\n\n```\nprint(\"{\")     -\u003e  error[RAD10009]: Unexpected '\"{\"'\nprint(\"{}\")    -\u003e  error[RAD10001]: Expected identifier\nprint(\"{ }\")   -\u003e  error[RAD10001]: Expected identifier\n```\n\n**Recommended rule, and the reason it is narrow.** Treat `{` as a literal only where it *cannot* open an interpolation: immediately followed by the string terminator, or containing nothing but whitespace. Everything else keeps erroring.\n\nThe tempting wider rule - fall back to literal whenever the interpolation fails to parse - is a trap. `print(\"{name\")` is a forgotten closing brace. Under the wider rule it silently prints `{name`, which converts a loud parse error into #153's own Tier 1 category. `{` followed by an identifier should stay an error precisely because it looks like an interpolation someone didn't finish.\n\nCases this fixes, all from #153:\n\n| Case | Today | After |\n|---|---|---|\n| `print(\"{\")` | error | prints `{` |\n| `t.starts_with(\"{\")` | error | works |\n| `` `\\.+*?()[]{}\\|^$` `` | error | works, the pair is empty |\n| `print(\"{name\")` | error | error, unchanged and deliberate |\n| `print(\"\\d{4}\")` | prints `\\d4` | unchanged - see the constant-literal lint card in p0-tier1 |\n\n**`{{` was considered and is not recommended.** It doesn't work today (`print(\"{{\")` errors), `\\{` already does the job and is documented at `SYNTAX.md:913`, and for brace-dense text `r\"...\"` beats both. Adding `{{` gives two escapes for one job.\n\n**The finding underneath this.** `\\{` works and is documented, and the author reached for `r`-strings twice without finding it. The escape isn't the gap - discovery is. That is the sibling card in this column, and it stays worth doing for the cases that remain errors after this one lands.\n\nMarked needs-decision because it changes what the grammar accepts. The recommendation above is the proposal, not a settled call.\n\n---\n\n### Orientation\n- The grammar lives in the separate `tree-sitter-rad` repo, checked out at `~/src/tree-sitter-rad`. Interpolation is a grammar-level construct, so this change likely starts there rather than in this repo.\n- `rts/` wraps that grammar and converts the CST to the AST in `rts/rl/`. `core/` then evaluates.\n- Per AGENTS.md, iterate on the grammar with a temporary `replace github.com/amterp/tree-sitter-rad =\u003e \u003clocal path\u003e` in go.mod, and remove it before committing - `./dev --release` refuses to release with one present.\n\n### Watch out for\nGrammar changes ripple into all five snapshot suites plus the VS Code extension's highlighting. Scope creep risk: it is tempting to also \"fix\" `\"\\d{4}\"` here. Don't - that is a valid interpolation, and it belongs to the lint card in p0-tier1.\n",
  "effort": "m",
  "id": "a_25S3ICERd",
  "labels": [
    "needs-decision"
  ],
  "position": "UUUUUUU",
  "source": [
    "friction-153"
  ],
  "title": "Let an unambiguous literal brace parse",
  "type": "enhancement",
  "updated_at_millis": 1786370375004,
  "history": [
    {"field":"column","value":"p1","at":1786353931892},
    {"field":"column","value":"approved","at":1786356958817},
    {"field":"column","value":"doing","at":1786359458204},
    {"field":"column","value":"done","at":1786370375004}
  ]
}