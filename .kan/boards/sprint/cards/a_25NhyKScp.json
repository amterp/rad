{
  "_v": 3,
  "alias": "http-add-timeout",
  "alias_explicit": false,
  "column": "p1",
  "created_at_millis": 1786343579201,
  "creator": "Alexander Terp",
  "description": "`core/requester.go` sets no client timeout anywhere, so a hung endpoint hangs the script. `mm-search` has logged a 601-second invocation against an 80-run average of 31 seconds.\n\n`shell_cmds.md` contemplated timeouts and punted them (\"Maybe don't worry for now\"). The agent-facing scripts change the calculus: a stuck call there blocks a tool call rather than a human who can ctrl-C.\n\n---\n\n### Orientation\n- All nine `http_*` builtins share a single `Execute` closure, built in a loop by `createHttpFunctions()` in `core/funcs.go`. `timeout=` gets implemented once, not nine times.\n- Signatures are not written in Go. Each method has its own `docs/funcs/http_\u003cmethod\u003e.md`, and the `## Signature` line in it IS what the type checker enforces. Format contract: `docs/funcs/README.md`.\n- The request goes out through the `RReq` seam (`core/global.go:44`) - `Requester.Request` at `core/requester.go:161`.\n\n### Watch out for\n- Do not set `Timeout` on the client. `getClient` hands back the process-global `http.DefaultClient` on the normal path and a Requester-cached client on the insecure path - a per-request value written onto either leaks into every other request. Derive a context instead: `Request` already receives `f.i.signals.Ctx()` (`core/funcs.go:1960`) and passes it to `http.NewRequestWithContext` (`core/requester.go:171`), so a `context.WithTimeout` off that gives per-request expiry and preserves the existing SIGINT cancellation.\n- Everything stays behind `RReq`. `core/testing/` injects a Requester with mocked responses (`core/testing/test_helpers.go:135-156`); calling `net/http` directly from the func body bypasses the seam and breaks determinism.\n- `--mock-response` short-circuits at `core/requester.go:239-241`, before `client.Do`. A snapshot case can prove the argument parses and threads through, but it cannot make a real timeout fire - test the expiry itself at the Go level against `Requester`.\n",
  "effort": "s",
  "id": "a_25NhyKScp",
  "labels": [
    "ai-hi"
  ],
  "position": "U",
  "source": [
    "takehome"
  ],
  "title": "http: add timeout=",
  "type": "feature",
  "updated_at_millis": 1786355430656,
  "history": [
    {"field":"column","value":"p1","at":1786343579201}
  ]
}