{
  "_v": 3,
  "alias": "add-did-you-mean-for",
  "alias_explicit": false,
  "column": "p2",
  "created_at_millis": 1786343579769,
  "creator": "Alexander Terp",
  "description": "Top finding of the clig.dev conformance audit filed 2026-07-14. Neither layer suggests corrections:\n\n- `rad chek` gives a bare \"Unknown file or command\"\n- `--frmat` gives a bare \"unknown flag\" from `ra`\n\nBoth pieces already exist: `core/fuzzy.go` holds Levenshtein machinery, and `ra` exposes `ParseHooks.ErrorHint`. Script-level identifiers already get did-you-mean; the CLI surface doesn't.\n\nSecond finding from the same card, cheap and adjacent: global flags only parse after the script path, so `rad --color never script.rad` fails.\n\n---\n\n### Orientation\n\nThis is three small independent changes in two different layers. They can ship separately.\n\n- **`rad chek`** - the \"Unknown file or command\" error is Rad's own, at `core/runner.go:146`, the last line of `detectInvocationType`. The candidate set is right there: `core.CmdsByName`, built in `core/cmds.go:42-56` from eight embedded commands (`new`, `docs`, `check`, `fmt`, `home`, `gen-id`, `stash`, `explain`).\n- **`--frmat`** - the \"unknown flag\" error comes from `ra`, not from Rad (`ra_cmd_parse.go:624` in the module cache: `fmt.Errorf(\"unknown flag: --%s\", flagName)`). Rad already has a hook into ra's error path.\n- **Global flags before the script path** - `core/runner.go:116-120`. Any first arg starting with `-` short-circuits to `NoScript`, so `rad --color never script.rad` never finds the script. There is already a `todo` on line 117 flagging the adjacent `rad -- myscript` case.\n\n### Where to start\n\n- `core/fuzzy.go` - `Levenshtein(a, b string) int` and `FuzzyMatchFold(source, target string) bool`. Tests in `core/fuzzy_test.go`. The checker has its own suggestion logic at `rts/check/suggestion.go`; match its threshold behavior rather than inventing a new one, so the CLI and script layers feel the same.\n- `core/runner.go:242-266` - Rad already sets `ra.ParseHooks{PostParse, ErrorHint}` on the root command. `ErrorHint` returns a string that ra prints between the error and the usage block. `parseErrorHint` (`core/runner.go:276-294`) is the implementation and today handles exactly one case: `*ra.TooManyPositionalArgsError`. Extending it is the whole job for the flag half.\n- `core/flags_global.go` - flag name consts (lines 8-27) and `CreateAndRegisterGlobalFlags` (line 73). For the global-flags-before-script fix you need this table anyway: you cannot simply skip leading `-` tokens, because flags like `--color` and `--mock-response` take a value and `GlobalFlagScopes` is the only thing that knows arity.\n\n### How to verify\n\n```sh\nmake build\n./bin/radd chek                       # expect a did-you-mean for `check`\n./bin/radd script.rad --frmat json    # expect a did-you-mean for the script's own flag\n./bin/radd --color never script.rad   # expect it to run, not to print help\ngo test ./core/testing/ -run TestSnapshots -update=cmds\ngo test ./core/testing/ -run TestSnapshots -update=args\nmake test\n```\n\n### Done when\n\n- A near-miss command name suggests the right embedded command; a nonsense argument still gets the plain \"Unknown file or command\".\n- A near-miss flag suggests the right flag, for both global flags and script-declared args.\n- `rad --color never script.rad` and `rad script.rad --color never` behave identically, with a snapshot pinning it.\n- No suggestion is emitted when nothing is close - a wrong guess is worse than none.\n\n### Watch out for\n\nra's unknown-flag error is a bare `fmt.Errorf`, not a typed error, so `errors.As` cannot reach it the way `*ra.TooManyPositionalArgsError` can. String-matching ra's message from Rad is exactly the \"workaround filed in the wrong repo\" that `AGENTS.md` warns about. `ra` is first-party (`go.mod:12` pins `v0.8.1`, checkout at `~/src/ra`): add a typed `UnknownFlagError` upstream, iterate with a local `replace` directive, and remove the directive before committing. Tagging and releasing `ra` is the maintainer's call, so flag that this half cannot fully land without a release.\n\nra also already knows the full flag set at the point of failure, which makes \"should the suggestion live in ra instead of Rad?\" a real question worth asking before writing code in Rad.",
  "effort": "s",
  "id": "a_25NhzEX5P",
  "labels": [
    "ai-hi"
  ],
  "position": "UUUU",
  "source": [
    "clig-audit"
  ],
  "title": "Add did-you-mean for CLI typos",
  "type": "enhancement",
  "updated_at_millis": 1786353910689,
  "history": [
    {"field":"column","value":"p2","at":1786343579769}
  ]
}