{
  "_v": 3,
  "alias": "add-url-encode",
  "alias_explicit": false,
  "column": "p1",
  "created_at_millis": 1786343579561,
  "creator": "Alexander Terp",
  "description": "For URLs that never pass through `http_*` and so never reach `sanitizeUrlString`. `vul:39-42` hand-encodes `%40` for a URL it hands to `open`; `hub:43` does the same kind of thing.\n\n---\n\n### Orientation\n- Ordinary built-in function, so the docs-first flow applies in full: the `## Signature` line in `docs/funcs/\u003cname\u003e.md` IS the type checker's signature. There is no parallel definition in Go.\n- The encoder already exists in the repo: `safeQueryEscape` (`core/requester.go:271-275`) is `url.QueryEscape` with `+` normalized to `%20`. Matching its behavior keeps `url_encode()` output consistent with what `sanitizeUrlString` produces for URLs that do go through `http_*`.\n\n### Watch out for\n- Decide up front whether this encodes a query *component* or a whole *path* segment - they differ on `/` and `+`. `safeQueryEscape` is component-flavored. If both are wanted, a named argument beats two functions, but say which is the default in the doc.\n- Round-tripping matters: output from `url_encode()` fed into an `http_*` url goes through `sanitizeUrlString`, which decodes with `PathUnescape` and re-encodes (`core/requester.go:281-321`). Confirm the pair is idempotent rather than double-encoding.\n",
  "effort": "xs",
  "id": "a_25NhyuSXu",
  "labels": [
    "ai-hi"
  ],
  "position": "UUUUUUUU",
  "source": [
    "takehome"
  ],
  "title": "Add url_encode()",
  "type": "feature",
  "updated_at_millis": 1786355430967,
  "history": [
    {"field":"column","value":"p1","at":1786343579561}
  ]
}