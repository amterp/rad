{
  "_v": 3,
  "alias": "make-rad-check-from",
  "alias_explicit": false,
  "column": "p1",
  "created_at_millis": 1786343579508,
  "creator": "Alexander Terp",
  "description": "The feature ships today and nobody found it. Three of the review's six assessors independently proposed it, two of them as a new feature - and the assessor that had the documenting page saved to disk the whole time still hand-rolled jq plus per-file checks. Its round-2 critic called that the opposite of motivated reasoning. This is a discoverability failure, not a capability gap.\n\nDiscoverability shapes the reports sketched: a rate-limited hint when an invoked script has outstanding check errors, an after-N-invocations variant, and a post-upgrade hint (\"rad was upgraded across a breaking version; run `rad check --from-logs 90d`\").\n\nFour real gaps beyond discoverability:\n\n- **Directory scan.** Nine of eleven broken scripts in one corpus have zero log entries. Three of seven agent-facing bin tools have never been logged and are structurally invisible to a log-keyed sweep.\n- **Per-diagnostic detail.** The sweep prints counts, so \"why is it broken\" still means re-running per script.\n- **`rad_version`-era reporting.** The log already records the interpreter version. Surfacing \"broke since v0.7\" is how the review dated `argus` to v0.9.0.\n- **Near-miss shebang detection.** `sess` was invisible to every sweep because its shebang says `radd`. A real Rad script with 11 diagnostics, unselectable by anything keyed on the shebang, found only by reading a directory listing.\n\nA `--fix` codemod is the natural follow-on and is deliberately not in this card. Two design inputs for when it comes: some rewrites must insert lines (`request` becomes `rad` plus `noprint`), and pre-v0.9 grammar deaths that surface as generic \"Invalid syntax\" aren't auto-fixable without an old-grammar parser.\n\n---\n\n### Orientation\n- `rad check` is itself a Rad script: `core/embedded/check`, compiled into the binary. Its args block is lines 5-11 and the `--from-logs` branch is line 14.\n- The heavy lifting is an **internal** builtin, `_rad_check_from_logs`, implemented at `core/funcs_internal_check_from_logs.go:52`. Internal `_rad_*` functions do **not** follow the docs-first flow - their signatures are hand-listed in `rts/signatures.go` (this one at `rts/signatures.go:71`, the sibling `_rad_run_check` at `:69`) and they deliberately have no `docs/funcs/` entry.\n- Adding a new internal function means: const in `core/funcs.go` (the `INTERNAL_FUNC_*` block, e.g. `INTERNAL_FUNC_CHECK_FROM_LOGS` at `core/funcs.go:150`), a `BuiltInFunc` in `core/funcs_internal.go`, and a hand-written line in `rts/signatures.go`.\n\n### Where to start\n- `core/funcs_internal_check_from_logs.go:283-314` - `parseInvocationLogs`, and `:318` - `processLogFile`. This is the log-keyed sweep the card wants to widen.\n- `core/funcs_internal_check_from_logs.go:30-33` - `scriptInfo`, which today keeps only `Path` and `LastOccurrence`.\n- `core/logging_invocation.go:45-51` - `InvocationLogEntry`. The premise checks out: it already records `RadVersion` as `rad_version`, so \"broke since v0.7\" reporting needs no new data collection, just threading the field through `scriptInfo` and the result table.\n- `core/funcs_internal_check_from_logs.go:100-104` - the per-script loop, including the `com.IsRegularFile` skip. A directory scan slots in as an alternative source feeding the same `[]scriptInfo`.\n- `core/funcs_internal_check_from_logs.go:43-48` - `scriptResult` and `diagCounts`. Per-diagnostic detail means carrying the `check.Diagnostic` values through rather than collapsing to counts here.\n- For the near-miss shebang case: there is no shebang filter in the log path today (the sweep is keyed purely on logged paths), so shebang detection is new logic that a directory scan will need. `rts` can read a shebang - see `Test_Tree_CanGetShebang` in `rts/test/rts_test.go:35`.\n- Discoverability hooks: `core/runner.go:72` is where invocation logging is gated per run, and `RegisterInvocationLogging` (`core/logging_invocation.go:11`) registers a pre-exit callback - that is the natural place for a rate-limited nudge.\n- Docs to update: `docs-web/docs/guide/config.md:24-43`, `docs-web/docs/guide/built-in-commands.md:48-52`, `docs-web/docs/migrations/index.md:31-68`.\n\n### How to verify\n```sh\nmake build\ngo test ./core/testing/ -run TestSnapshots -update=misc/check\ngo test ./core/testing/\nmake generate \u0026\u0026 make verify-generated\n```\nThe rendered `rad check` output is snapshotted at `core/testing/snapshots/misc/check.snap`. `make generate` matters here because `core/embedded_docs/` is generated from the docs-web pages.\n\n### Done when\n- A directory scan is available and finds scripts with zero log entries, including ones whose shebang is a near-miss (`radd`) rather than `rad`.\n- Per-diagnostic detail is reachable without re-running per script.\n- The `rad_version` at last invocation is surfaced, so \"broke since vX\" is answerable from the sweep.\n- Whatever discoverability nudge ships is rate-limited and does not fire on every invocation - a hint printed on all 22k runs of a launchd script is worse than no hint.\n- Guide pages updated so the feature is findable from the docs, which is the actual complaint on this card.\n\n### Watch out for\n- Any nudge printed at script exit runs on the hot path of every single `rad` invocation. `RegisterInvocationLogging` already guards on `RConfig.InvocationLogging.Enabled`; a nudge needs its own persisted rate-limit state and its own config switch, or it becomes the next thing people disable.\n- Time and filesystem access must go through the seams: `RClock` is already used for the cutoff (`core/funcs_internal_check_from_logs.go:296`), not `time.Now()`. Keep new code consistent so `core/testing/` stays deterministic.\n- Directory scanning is unbounded work in a way the log sweep is not. Decide on depth limits, ignore rules, and symlink handling before writing the walk.\n- A `--fix` codemod is explicitly out of scope for this card - don't let it creep in.",
  "effort": "m",
  "id": "a_25NhyoUFV",
  "labels": [
    "ai-lo"
  ],
  "position": "UUUUUUU",
  "source": [
    "takehome"
  ],
  "title": "Make rad check --from-logs findable, and add a directory scan",
  "type": "enhancement",
  "updated_at_millis": 1786353834066,
  "history": [
    {"field":"column","value":"p1","at":1786343579508}
  ]
}