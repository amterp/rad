---
A very simple example.


A simple script to work through all
the things that Rad's interpreter
needs to be able to do.

Long part can be two paragraphs, even!
---

args:
    repo string # Repo!
    limit int = 20 # Limit!

a = 1
b = "hi"
c = true

Name = json[].commit.author.name
Date = json[].commit.author.date

url = "https://api.github.com/repos/{repo}/commits?per_page=2"
