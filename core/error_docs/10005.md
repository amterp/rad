# RAD10005: Missing Close Parenthesis

The parser expected a `)` but didn't find one.

## Example

```rad
# Wrong
print("hello"

# Correct
print("hello")
```

Check that each `(` has a matching `)` and that they're properly nested.
