# RAD10007: Missing Close Brace

The parser expected a `}` but didn't find one.

## Example

```rad
# Wrong
data = {"name": "Alice"

# Correct
data = {"name": "Alice"}
```

This also applies to string interpolations:

```rad
# Wrong
message = "Hello, {name"

# Correct
message = "Hello, {name}"
```
