# RAD10015: Missing Arrow

The parser expected `->` but didn't find one.

## Example

```rad
# Wrong
fn get_value() int:
    return 42

# Correct
fn get_value() -> int:
    return 42
```

The `->` separates function parameters from the return type.
