# RAD10006: Missing Close Bracket

The parser expected a `]` but didn't find one.

## Example

```rad
# Wrong
items = [1, 2, 3

# Correct
items = [1, 2, 3]
```

Check that each `[` has a matching `]`, especially in multi-line lists.
